/* v1.46.0 — Billplz was replaced by Bayarcash (worker/src/bayarcash.ts).
   This file is intentionally empty and can be deleted; nothing imports it.
   It is left as a stub only because the deploy tooling cannot delete files
   on the CEO's computer — remove it by hand when convenient. */
export {};
